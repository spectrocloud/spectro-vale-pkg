## Pass Cases

Use the API to manage clusters.

The CLI supports multiple commands.

Monitor CPU usage on the dashboard.

Open the GUI to view status.

Connect to AWS using your credentials.

The UI shows real-time metrics.

Create a VM with the required specs.
